import React from 'react'
import ReactDOM from 'react-dom/client'
import GMMCalculator from './gmm-calculator-form'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GMMCalculator />
  </React.StrictMode>,
)
